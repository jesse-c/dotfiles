#!/bin/bash

{{ if lookPath "op" }}

# 1Password CLI version: {{ output "op" "--version" | trim }}

op completion fish > ~/.config/fish/completions/op.fish

{{ else }}

echo "op is missing"

{{ end }}
