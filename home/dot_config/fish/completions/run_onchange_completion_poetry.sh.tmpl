#!/bin/bash

{{ if lookPath "poetry" }}

# poetry version: {{ output "poetry" "--version" | trim }}

poetry completions fish > ~/.config/fish/completions/poetry.fish

{{ else }}

echo "poetry is missing"

{{ end }}
