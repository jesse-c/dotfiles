#!/bin/bash

{{ if lookPath "sg" }}

# ast-grep CLI version: {{ output "sg" "--version" | trim }}

sg completions fish > "$HOME/.config/fish/completions/sg.fish"

{{ else }}

echo "sg is missing"

{{ end }}
