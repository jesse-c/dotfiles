#!/bin/bash

{{ if lookPath "srgn" }}

# srgn version: {{ output "srgn" "--version" | trim }}

srgn --completions fish >"$HOME/.config/fish/completions/srgn.fish"

{{ else }}

echo "srgn is missing"

{{ end }}
