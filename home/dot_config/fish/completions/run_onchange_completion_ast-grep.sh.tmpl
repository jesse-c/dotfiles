#!/bin/bash

{{ if lookPath "ast-grep" }}

# ast-grep CLI version: {{ output "ast-grep" "--version" | trim }}

ast-grep completions fish > "$HOME/.config/fish/completions/ast-grep.fish"

{{ else }}

echo "ast-grep is missing"

{{ end }}
