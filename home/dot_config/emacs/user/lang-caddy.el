;;; lang-caddy.el --- -*- lexical-binding: t; -*-

(use-package caddyfile-mode
  :mode (("Caddyfile\\'" . caddyfile-mode)
         ("caddy\\.conf\\'" . caddyfile-mode)))

(provide 'lang-caddy)

;;; lang-caddy.el ends here
